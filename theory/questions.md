# Полный список вопросов

Типичные вопросы по Go и смежным темам на собеседованиях. Ответы — в соответствующих файлах `theory/`.

## Основы языка Go (`01-basics.md`)

1. Какие основные принципы и философия языка Go?
2. В чем разница между `var`, `:=` и `new()`?
3. Что такое zero value в Go и для каких типов?
4. Как работает `defer` и в каком порядке выполняются `defer`-вызовы?
5. Что такое `init()` функция и когда она вызывается?
6. Как работает `recover()` и где его можно использовать?
7. Что такое `iota` и как она работает?

## Структуры данных (`02-data-structures.md`)

8. Как устроены слайсы (slices) в Go? Что такое capacity и length?
9. Как работает функция `append()` и когда происходит переаллокация?
10. Как устроена `map` в Go? Какая структура данных лежит в основе?
11. Как решаются коллизии в `map`?
12. Какая сложность операций `get`, `put`, `delete` в `map`?
13. Что такое `sync.Map` и когда его использовать вместо обычной `map`?
14. Почему нельзя использовать `map` как ключ в другой `map`?
15. В чем разница между массивом и слайсом?
16. Что произойдет при копировании слайса? Копируются ли элементы?
17. Что из себя представляют строки в Go? Как они устроены?

## Горутины и конкурентность (`03-concurrency.md`)

18. Что такое горутина и чем она отличается от системного потока?
19. Как работает планировщик горутин (Goroutine Scheduler) в Go?
20. Что такое M:N модель и как она реализована в Go?
21. Что такое `context` и для чего он используется?
22. Как правильно отменять горутины через `context`?
23. Что такое `context.WithTimeout` и `context.WithCancel`?
24. Что такое `context.WithValue` и когда его использовать?
25. Что такое `sync.WaitGroup` и как его использовать?
26. В чем разница между `sync.Mutex` и `sync.RWMutex`?
27. Что такое `sync.Once` и когда его применять?
28. Что такое `atomic` операции и когда их использовать?
29. Что такое data race и как его обнаружить?
30. Как работает `-race` флаг в Go?
31. Что такое goroutine leak и как его предотвратить?
32. Что произойдет если main функция завершится раньше горутин?
33. Как ограничить количество одновременно работающих горутин?
34. Что такое GOMAXPROCS и как это влияет на производительность?
35. Можно ли получить ID горутины и стоит ли это делать?
36. Как безопасно передавать данные между горутинами?

## Каналы (Channels) (`04-channels.md`)

37. Как работают каналы в Go? Буферизованные vs небуферизованные
38. Что произойдет при записи в закрытый канал?
39. Что произойдет при чтении из закрытого канала?
40. Как правильно закрывать каналы?
41. Что такое `select` и как он работает?
42. Что такое `default` в `select` и когда его использовать?
43. Как использовать `select` для таймаутов?
44. Что такое `nil` канал и как он ведет себя в `select`?
45. Как реализовать паттерн fan-in и fan-out с помощью каналов?

## Интерфейсы (`05-interfaces.md`)

46. Как работают интерфейсы в Go? Что такое duck typing?
47. Что такое пустой интерфейс `interface{}` и когда его использовать?
48. В чем разница между `any` и `interface{}`?
49. Как работает type assertion и type switch?
50. Что такое embedding интерфейсов?
51. Можно ли реализовать интерфейс для типа из другого пакета?
52. Что такое `io.Reader` и `io.Writer`?

## Управление памятью (`06-memory-management.md`)

53. Как работает сборщик мусора (GC) в Go?
54. Что такое escape analysis и как он работает?
55. Когда переменная попадает в кучу (heap), а когда остается в стеке?
56. Что такое выравнивание (alignment) структур и почему это важно?
57. Как оптимизировать использование памяти в Go?

## Ошибки и обработка ошибок (`07-error-handling.md`)

58. Как обрабатываются ошибки в Go? Почему нет исключений?
59. Что такое `error` интерфейс?
60. Как правильно оборачивать ошибки? `fmt.Errorf` vs `errors.Wrap`
61. Что такое `errors.Is` и `errors.As`?
62. Как создать кастомный тип ошибки?
63. Как работают `panic` и `recover`? Чем panic отличается от `error`?

## Тестирование (`08-testing.md`)

64. Как писать unit-тесты в Go?
65. Что такое table-driven tests?
66. Как использовать `t.Helper()`?
67. Что такое `testify` и для чего он нужен?
68. Как писать benchmark тесты?
69. Как измерять покрытие кода тестами?
70. Что такое `httptest` и как тестировать HTTP handlers?

## Пакеты и модули (`09-packages-modules.md`)

71. Как работает система модулей в Go (go.mod)?
72. Как работает `go.sum` файл?
73. Что такое `internal` пакет?

## Системный дизайн и архитектура (`11-system-design.md`)

74. Как спроектировать высоконагруженную систему на Go?
75. Какие паттерны проектирования часто используются в Go?
76. Как обеспечить отказоустойчивость микросервисов?
77. Как реализовать circuit breaker на Go?

## Работа с базами данных (`12-databases.md`)

78. Как работать с SQL базами данных в Go?
79. Что такое `database/sql` и как он работает?
80. Как работают connection pools в Go?
81. Как использовать транзакции?
82. Какие ORM используются в Go? (GORM, sqlx и т.д.)

## HTTP и веб-разработка (`13-http-web.md`)

83. Как работает `net/http` пакет?
84. В чем разница между `http.Handle` и `http.HandleFunc`?
85. Как создать middleware для HTTP handlers?
86. Как работает `context` в HTTP запросах?
87. Как реализовать graceful shutdown HTTP сервера?

## Производительность и профилирование (`14-performance.md`)

88. Какие инструменты для профилирования есть в Go?
89. Как использовать `pprof` и профилировать CPU/память?
90. Что такое `go tool trace`?
91. Как оптимизировать производительность Go приложений?

## Безопасность (`15-security.md`)

92. Какие уязвимости безопасности характерны для Go приложений?
93. Как защититься от SQL инъекций?
94. Как безопасно работать с секретами?
95. Что такое `unsafe` пакет и когда его использовать?

## Сети и сетевые протоколы (`16-networking.md`)

96. Что такое модель TCP/IP?
97. В чем разница между TCP и UDP?
98. Как работает TCP handshake (трехстороннее рукопожатие)?
99. Что такое HTTP и какие у него методы?
100. В чем разница между HTTP/1.1, HTTP/2 и HTTP/3?
101. Что такое HTTPS и как работает SSL/TLS?
102. Что такое DNS и как работает разрешение имен?
103. Что такое REST API? Какие у него принципы?
104. Что такое WebSocket и чем отличается от HTTP?
105. Что такое порты и для чего они нужны?
106. Что такое NAT и для чего он используется?
107. Что такое load balancer и какие типы бывают?

## Операционные системы (`17-operating-systems.md`)

108. Что такое процесс и поток? В чем разница?
109. Как процессы взаимодействуют между собой (IPC)?
110. Что такое системный вызов (syscall)?
111. Что такое виртуальная память и зачем она нужна?
112. Что такое page fault и когда он возникает?
113. Что такое context switching и почему он дорогой?
114. Что такое файловый дескриптор?
115. Что такое signals в Unix/Linux?
116. Что такое Copy-on-Write (COW)?

## Базы данных (общие вопросы) (`18-databases-general.md`)

117. Что такое ACID и для чего нужны эти свойства?
118. Что такое транзакция и зачем она нужна?
119. Какие уровни изоляции транзакций существуют?
120. Что такое индекс и для чего он нужен?
121. Какие типы индексов существуют?
122. Что такое нормализация БД и какие формы существуют?
123. Что такое JOIN и какие типы бывают?
124. Что такое первичный ключ (Primary Key) и внешний ключ (Foreign Key)?
125. Что такое репликация и какие типы бывают?
126. Что такое шардирование (sharding)?
127. В чем разница между SQL и NoSQL базами данных?
128. Что такое CAP теорема?
129. Что такое денормализация и когда ее применять?
130. Что такое explain plan и для чего используется?
131. Что такое deadlock в БД и как его избежать?
132. Что такое оптимистичная и пессимистичная блокировки?
133. Что такое MVCC?
134. Что такое Write-Ahead Logging (WAL)?
135. Что такое партиционирование (partitioning) таблиц?

## SOLID принципы (`19-solid-principles.md`)

136. Что такое SOLID принципы и зачем они нужны?
137. Single Responsibility Principle (SRP)
138. Open/Closed Principle (OCP)
139. Liskov Substitution Principle (LSP)
140. Interface Segregation Principle (ISP)
141. Dependency Inversion Principle (DIP)
142. Как SOLID помогает в тестировании и какие компромиссы?

## Apache Kafka (`20-kafka.md`)

143. Что такое Apache Kafka и для чего используется?
144. Из каких компонентов состоит Kafka?
145. Что такое topic, partition и offset?
146. Как обеспечивается порядок сообщений в Kafka?
147. Что такое consumer group и как работает rebalance?
148. Как устроена репликация в Kafka? Что такое ISR?
149. Чем отличаются at-least-once, at-most-once и exactly-once?
150. Что такое идемпотентный producer?
151. Как работают Kafka transactions?
152. В чем разница между Kafka и RabbitMQ?
153. Что такое KRaft и чем он заменил ZooKeeper?
154. Что такое consumer lag и как с ним бороться?

## Docker и Kubernetes (`21-docker-kubernetes.md`)

155. Чем контейнер отличается от виртуальной машины?
156. Что такое image и container?
157. Как устроен Dockerfile? Что такое слои и cache?
158. `CMD` vs `ENTRYPOINT`, `COPY` vs `ADD`
159. Volume vs bind mount — когда что
160. Как уменьшить размер образа Go-приложения?
161. Что такое multi-stage build и зачем он Go-разработчику?
162. Docker Compose — зачем нужен
163. Как пробросить переменные окружения и секреты в контейнер
164. Почему в контейнере не должно быть root-пользователя
165. Зачем нужен Kubernetes? Что он даёт поверх Docker
166. Pod, Deployment, Service — что делает каждый
167. ReplicaSet vs Deployment vs StatefulSet
168. ConfigMap и Secret — зачем и чем отличаются
169. Ingress — что это и зачем
170. Liveness vs Readiness probe
171. Rolling update и rollback
172. Что такое requests/limits (CPU, memory)
173. Что происходит, когда pod падает
174. Namespace — зачем
175. HPA — горизонтальное автомасштабирование (базово)
176. Как Go-сервис деплоится в k8s: healthcheck, graceful shutdown, SIGTERM

## gRPC (`22-grpc.md`)

177. Что такое gRPC и чем отличается от REST?
178. Что такое Protocol Buffers (protobuf)?
179. Какие типы вызовов есть в gRPC?
180. Как работают deadline и cancellation в gRPC?
181. Как обрабатывать ошибки в gRPC?
182. Что такое interceptors (middleware) в gRPC?
183. gRPC vs REST — когда что выбрать?
184. Как тестировать gRPC-сервисы в Go?

## Observability (метрики, логи, трейсы) (`23-observability.md`)

185. Что такое observability и из чего она состоит?
186. Чем метрики отличаются от логов?
187. Что такое RED и USE методологии?
188. Как устроен Prometheus и pull-модель?
189. Какие метрики собирать у Go-сервиса?
190. Что такое distributed tracing?
191. Структурированное логирование в Go
192. Как профилировать Go-сервис в production?

## Сценарии и troubleshooting (`24-scenarios.md`)

193. Latency выросла после деплоя — с чего начать?
194. Растёт число горутин (goroutine leak) — как найти?
195. Pod в k8s убит OOMKilled — что делать?
196. Паника в горутине — что будет с сервисом?
197. Connection pool к PostgreSQL исчерпан — почему и как починить?
198. Клиент дважды отправил оплату — как защититься?
199. Два пользователя бронируют последнее место одновременно
200. Thundering herd при протухании кеша
201. Kafka consumer lag растёт — план действий
202. Дубликаты сообщений из Kafka — как обрабатывать?
203. 502/503 под нагрузкой — где искать
204. Rolling update в k8s, но был даунтайм
205. Сервис не подключается к БД после деплоя в k8s
206. N+1 запросов к БД — как обнаружить и исправить
207. Как спроектировать rate limiter для публичного API?
208. Миграция с монолита на микросервисы без даунтайма (Strangler)
209. Медленный endpoint — пошаговая оптимизация

## Magnit Tech (`magnit-tech/`)

Отдельный блок вопросов с собесов Magnit Tech. Ответы — в `theory/magnit-tech/`.

- **M1** Как работают atomic операции под капотом в Go? → `01-atomics.md`

- **M2** Как работают panic в Go и чем отличаются от error? → `02-panic-vs-error.md`

- **M3** Примеры аномалий на разных уровнях изоляции (SQL) → `03-isolation-levels.md`

- **M4** Почему в PostgreSQL нет первого уровня изоляции и как это решается? → `04-postgres-read-uncommitted.md`

- **M5** Как выбирать порядок колонок в составном индексе? → `05-composite-index.md`

- **M6** Rate limiter на PostgreSQL и в Go на atomic (почему не `int`) → `06-rate-limiter.md`

## Avito (`avito/`)

Отдельный блок вопросов с собесов Avito (Backend screening + платформа Go). Ответы — в `theory/avito/`.

- **A1** Go: слайс, map, set, каналы, context, panic → `01-screening-theory.md`

- **A2** Что такое горутины и как они соотносятся с потоками ОС? → `02-goroutines-and-runtime.md`

- **A3** Каналы → `03-channels-sync-interfaces.md`

- **A4** Указатели: что выведет программа? → `04-code-review-puzzles.md`

- **A5** Сборка сниппета товара → `05-snippet-build.md`

- **A6** Прогноз погоды при 10k RPS → `06-weather-cache.md`

- **A7** IO-bound: 10 000 последовательных «сетевых» вызовов → `07-network-parallel-timeout.md`

- **A8** Чемпионат по шагам → `08-algos.md`

